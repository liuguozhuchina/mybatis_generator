<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.huayu.entity.personal.UserAnswerDao">
    <resultMap id="userAnswer" type="com.huayu.entity.personal.UserAnswer">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="question_id" property="questionId"/>
        <result column="answer" property="answer"/>
        <result column="type" property="type"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <!-- Select SQL -->
    <sql id="selector">
        SELECT
        <if test="fields != null and fields!='' ">
        ${fields}
        </if>
        <if test="fields == null or fields=='' ">
        id,user_id,question_id,answer,type,update_time,create_time
        </if>
        FROM user_answer
    </sql>

    <!-- Where SQL -->
    <sql id="Listwhere">
     <where>
        <if test="id!= null ">
            AND id= #{id}
        </if>
        <if test="userId!= null ">
            AND user_id= #{userId}
        </if>
        <if test="questionId!= null ">
            AND question_id= #{questionId}
        </if>
        <if test="answer!= null and answer!='' ">
            AND answer= #{answer}
        </if>
        <if test="type!= null ">
            AND type= #{type}
        </if>
        <if test="updateTime!= null ">
            AND update_time= #{updateTime}
        </if>
        <if test="createTime!= null ">
            AND create_time= #{createTime}
        </if>
     </where>
    </sql>

    <!-- Order SQL -->
    <sql id="Listorder">
        <if test="orderFields != null and orderFields.size >0">
            order by
            <foreach collection="orderFields" separator="," item="orderField">
            ${orderField.fieldName} ${orderField.order}
            </foreach>
        </if>
    </sql>

    <!-- Page limit SQL -->
    <sql id="ListLimit">
        <if test="startRow >=0">
         limit #{startRow},#{pageSize}
        </if>
    </sql>

    <!-- 添加 -->
    <insert id="addUserAnswer" parameterType="com.huayu.entity.personal.UserAnswer">
        INSERT INTO  user_answer
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id!= null ">
             id,
            </if>
            <if test="userId!= null ">
             user_id,
            </if>
            <if test="questionId!= null ">
             question_id,
            </if>
            <if test="answer!= null and answer!='' ">
             answer,
            </if>
            <if test="type!= null ">
             type,
            </if>
            <if test="updateTime!= null ">
             update_time,
            </if>
            <if test="createTime!= null ">
             create_time,
            </if>
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            <if test="id!= null ">
              #{id},
            </if>
            <if test="userId!= null ">
              #{userId},
            </if>
            <if test="questionId!= null ">
              #{questionId},
            </if>
            <if test="answer!= null and answer!='' ">
              #{answer},
            </if>
            <if test="type!= null ">
              #{type},
            </if>
            <if test="updateTime!= null ">
              #{updateTime},
            </if>
            <if test="createTime!= null ">
              #{createTime},
            </if>
        </trim>
    </insert>

    <!-- 批量添加 -->
    <insert id="addUserAnswers" parameterType="java.util.List">
        INSERT INTO  user_answer(id,user_id,question_id,answer,type,update_time,create_time)
        VALUES
        <foreach collection="list" item="item" index="index" separator=",">
            (#{item.id},#{item.userId},#{item.questionId},#{item.answer},#{item.type},#{item.updateTime},#{item.createTime})
        </foreach>
    </insert>

    <!-- 根据主键删除 -->
    <delete id="deleteByKey" parameterType="long">
        DELETE FROM  user_answer
        WHERE   id=#{id}
    </delete>

    <!-- 根据主键批量删除 -->
    <delete id="deleteByKeys">
        DELETE FROM  user_answer
        WHERE    id in
        <foreach collection="list" separator="," item="id" open="(" close=")">
        #{id}
        </foreach>
    </delete>

    <!-- 根据条件删除 -->
    <delete id="deleteByQuery" parameterType="com.huayu.entity.personal.UserAnswer">
        DELETE FROM  user_answer
        <include refid="Listwhere" />
    </delete>

    <!-- 根据条件删除 -->
    <delete id="deleteByMap" parameterType="map">
        DELETE FROM  user_answer
        <include refid="Listwhere" />
    </delete>

       <!-- 根据主键更新 -->
    <update id="updateUserAnswerByKey" parameterType="com.huayu.entity.personal.UserAnswer">
        UPDATE user_answer
        <set>
            <trim suffixOverrides=",">
                <if test="id!= null ">
                id= #{id},
                </if>
                <if test="userId!= null ">
                user_id= #{userId},
                </if>
                <if test="questionId!= null ">
                question_id= #{questionId},
                </if>
                <if test="answer!= null and answer!='' ">
                answer= #{answer},
                </if>
                <if test="type!= null ">
                type= #{type},
                </if>
                <if test="updateTime!= null ">
                update_time= #{updateTime},
                </if>
                <if test="createTime!= null ">
                create_time= #{createTime},
                </if>
            </trim>
        </set>
        WHERE  id= #{id}
    </update>

    <!-- 根据map更新 -->
    <update id="updateUserAnswerByMap" parameterType="map">
        UPDATE user_answer
        <set>
            <trim suffixOverrides=",">
                <if test="id!= null" >
                id= #{id},
                </if>
                <if test="userId!= null" >
                user_id= #{userId},
                </if>
                <if test="questionId!= null" >
                question_id= #{questionId},
                </if>
                <if test="answer!= null and answer!=''" >
                answer= #{answer},
                </if>
                <if test="type!= null" >
                type= #{type},
                </if>
                <if test="updateTime!= null" >
                update_time= #{updateTime},
                </if>
                <if test="createTime!= null" >
                create_time= #{createTime},
                </if>
            </trim>
        </set>
        WHERE  id= #{id}
    </update>

    <!-- 根据主键查询 -->
    <select id="getUserAnswerByKey" resultMap="userAnswer" parameterType="com.huayu.entity.personal.UserAnswer">
        SELECT id,user_id,question_id,answer,type,update_time,create_time FROM user_answer
        WHERE  id= #{id}
    </select>

    <!-- 根据主键批量查询 -->
    <select id="getUserAnswersByKeys" resultMap="userAnswer" parameterType="map">
        SELECT id,user_id,question_id,answer,type,update_time,create_time FROM user_answer
        WHERE
    id in
        <foreach collection="list" separator="," item="id" open="(" close=")">
        #{id}
        </foreach>
    </select>

    <!-- 分页 -->
    <select id="getUserAnswerListWithPage" parameterType="map" resultMap="userAnswer">
        <include refid="selector" />
        <include refid="Listwhere" />
        <include refid="Listorder" />
        <include refid="ListLimit" />
    </select>

    <!-- 数据集合 -->
    <select id="getUserAnswerList" parameterType="map" resultMap="userAnswer">
        <include refid="selector" />
        <include refid="Listwhere" />
        <include refid="Listorder" />
    </select>

    <!-- 数据总数 -->
    <select id="getUserAnswerListCount" parameterType="map" resultType="int">
        SELECT count(1) FROM user_answer
        <include refid="Listwhere" />
    </select>



</mapper>



